node_modules/
dist/
dist-npm/
coverage/
*.tsbuildinfo

# Cloudflare
.wrangler/
.dev.vars

# fallow のローカルキャッシュ(コミットする baseline は fallow-baselines/)
.fallow/

# Homebrew formula は生成物(apps/cli/scripts/generate-formula.ts が Release の
# checksums.txt から作り、tap リポジトリへコピーする — docs/RELEASING.md)。
# 形の例は packaging/homebrew/maruhi.example.rb をコミットしている
packaging/homebrew/maruhi.rb

# Blume の生成ランタイム(apps/site — blume dev / build が毎回作り直す)
.blume/
.blume-verify/

# ダッシュボードのスクリーンショット(apps/web/test/screenshots.ts の出力 — PR に添付する一時物)
apps/web/screenshots/
