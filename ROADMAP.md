# maruhi ロードマップ

## Phase 0: 開発開始前(Bun 1.4 リリース + 数週間の待機期間)

- [x] 仕様の最終レビュー: 「実装開始前に要決定」3 件を決定(2026-08-01 所有者裁定): 環境モデル = プロジェクト × 環境 × エポック(CRYPTO_SPEC §3〜§5)、認可モデル = チェーン上の 4 role(CRYPTO_SPEC §6.2)、プロジェクトと組織の関係 = パーソナル org 自動作成(AUTH_SPEC §9-1)
- [x] 監査ログのスキーマ設計(記録イベント一覧、actor = 内部 user_id + 鍵フィンガープリント、DO 内 append-only 形式。「要ローテーション検出」の算出要件を満たすこと)(2026-08-02 完了。AUDIT_SPEC = セッション 06 起草、PR #18 のマージをもって所有者承認済み。チェックの付け忘れを 2026-08-03 に修正)
- [x] 暗号テストベクターの定義(実装より先にコミット)(2026-08-01 PR #11。2026-08-02 PR #12 で grant_server / revoke_server / change_role・認可系 negative・expected_head_states を補完)
- [x] 検証スパイク(使い捨て)3 本完了(2026-08-01。結果は docs/notes/spike-{a,b,c}.md):
  - スパイク A: funstack-static + funstack-router + Astryx(ADR-0013)→ Workers Static Assets。`"use client"` 境界、Navigation API 非対応ブラウザの劣化挙動、プリビルド CSS の静的配信 + 厳格 CSP、StyleX コンパイラ(xstyle 用)と Vite の組み合わせを確認
  - スパイク B: Effect v4 HttpApi + Durable Objects(ManagedRuntime パターン)+ vitest-pool-workers + Alchemy v2 / wrangler 両対応。実デプロイ検証は wrangler 経路成立(Alchemy 経路はユーザー API トークン切り替え待ち。docs/notes/session-03.md)
  - スパイク C: E2EE ラウンドトリップをブラウザ / Bun / workerd の 3 環境で確認。HPKE ライブラリ選定(未決事項 #1)= `hpke`(panva)で決定
- [x] npm `maruhi` プレースホルダ publish + org `maruhi`(@maruhi スコープ)作成(2026-08-01 完了。`maruhi@0.0.1`)
- [ ] ウォッチ: Drizzle v1 正式版(D1 / DO SQLite の Effect ドライバ有無)、Effect v4 安定版化(Bun 1.4 は 2026-08-24 の 1.4.0 到達で完了 — PR #91)

## Phase 1: MVP(クローズド開発)

**完了条件: 自分の全プロジェクトから .env を消せた**

- [x] E2EE コア(純粋 E2EE。サーバー鍵 = 選択的開示は実装しない、データ構造のみ確保)(2026-08-02 完了。PR #12 = packages/crypto: §2.1 エンコーダ / §3 鍵 / §4 変数暗号化 / §5 DEK ラップ / §6 チェーン検証・状態導出 / §8 リカバリーラップ、テストベクター全通過・4 実行環境 CI。§6.3 の DEK ラップ先一致検査・ヘッドゴシップは同期ロジックとして後続)
- [ ] メンバーシップログ(genesis + 単独ユーザー分。検証ロジック込み)※チェーンの暗号・検証層は PR #12、サーバー保存(プロジェクト DO)・追記 API(§6.4 検証 / CAS / 受理ポリシー)は PR #14 で完了(2026-08-02)。クライアント同期のうち §6.3 の DEK ラップ先一致検査は PR #25 で完了(2026-08-03)。残りはヘッドゴシップ(Phase 2)
- [x] CLI: `maruhi run`(メモリ注入)、`push` / `pull`、device flow ログイン、OS キーチェーン(2026-08-03 完了。PR #25 = apps/cli: login〔device flow〕/ logout / key / project init|verify / env create / pull [--show] / push / run / config、OS キーチェーン = Bun.secrets、§5.1 配布時検証・§6.3 同期検査のクライアント実装込み。§6.3 ローカル床(巻き戻し・欠落・前進注入〔値〕の永続検出)は PR #33 = セッション 16 / 真正性シリーズ実装 PR-4 で完了(2026-08-04)。CLI 配布・Windows 対応は Phase 2、初回セットアップウィザード・リカバリーは別項目のまま)
- [x] サーバー: プロジェクト DO、D1、HttpApi、監査ログ(append-only)※ DO・D1(Drizzle v1)・HttpApi に加え、認証・アイデンティティ基盤(AUTH_SPEC 本実装: GitHub OAuth web / device 交換、DB バックセッション、maruhi 発行トークン、パーソナル org、チェーン API の認証・認可 = AUTH_SPEC §11)は PR #16 で完了(2026-08-02)。変数値・環境・DEK API(AUTH_SPEC §12)と監査ログ(project DO 側: AUDIT_SPEC §3.3 / §3.4 / §5.1。同 PR のマージをもって AUDIT_SPEC は所有者承認済み)は PR #18 で完了(2026-08-02)。セッション 07 レビュー裁定 3 件の実装(3-D = ラップの suite 保存、F 先行分 = DEK ラップ行数上限、2-D = 修復経路〔ラップ削除 → 再登録〕+ dek 監査イベント)は PR #20 で完了(2026-08-02)。DEK ラップ登録署名(裁定 2-E = CRYPTO_SPEC §5.1。同 PR のマージをもって §5.1 は所有者承認済み)は PR #21 で完了(2026-08-03)。値・DEK・メタデータの真正性(CRYPTO_SPEC 0.4 改訂 = セッション 12。PR #27 マージで承認)の実装は PR #28(DEK コミットメント / create_environment・rotate_epoch の複合受理)・PR #30(値署名 / 認可時点の二重判定 / prev 連鎖・fork 証拠化)・PR #31(メタデータステートメント / 認証済み名前解決)で完了(2026-08-04。crypto / api-schema / server / CLI 横断)。監査ログの D1 側(認証・org 系 = AUDIT_SPEC §3.1〜§3.2 / §5.2 案 A = user_audit_events / org_audit_events、auth.recovery_* の記録 = AUTH_SPEC §13-5 申し送り解消込み)は PR #42 = セッション 21 で完了(2026-08-10)。残りは読み取り API(Phase 2 の監査ログ UI と同時に設計)。pull のメタデータのみモード(AUTH_SPEC §12-7 改訂 = var.read を記録しない意味論の明文化 + CLI push の名前解決切り替え・DEK 二重取得解消)は PR #41 = セッション 20 で完了(2026-08-10。session-11 §5 の裁定済み後続 PR 3 本はこれで全完了)
- [x] リカバリーコード(保存確認・印刷 / 保管リマインダ等の紛失対策 UX 込み — ADR-0014)(2026-08-09 セッション 18 で完了。CRYPTO_SPEC §8 のサーバー保存・配布面 = AUTH_SPEC §13 起草(登録・再発行 = 置換 upsert / 取得レート制限 = 1 時間 5 回 / status)、D1 `recovery_wraps` + ハンドラ、CLI = `key generate` への発行組み込み(Base32 13 グループ表示・最終グループ再入力の保存確認・エージェント環境スキップ)+ `key recovery`(再発行)+ `key recover`(復元)+ `key show` / login の保管リマインダ。auth.recovery_* の監査記録は D1 側監査基盤(下記残項目)と同時に実装する申し送り)
- [x] セルフホスト初回セットアップウィザード(GitHub OAuth App の作成案内 + client_id/secret 登録。AUTH_SPEC §3 参照)(2026-08-10 セッション 19 で完了。形 = `docs/SELF_HOSTING.md` の検証済み runbook(ADR-0014「セルフホストは上級者経路」)+ 公開設定エンドポイント `GET /auth/config`(セッション 11 裁定 B の実装 — login の client_id 自動解決、CLI 設定は server 1 項目で足りる)+ 未設定検出(プレースホルダ → 503 SetupIncomplete で fail-closed)。ランタイムの client_id/secret 登録 API は先着者乗っ取り経路のため不採用 — AUTH_SPEC §3)
- [x] Deploy to Cloudflare / wrangler 一発デプロイの検証(2026-08-10 セッション 19 で完了。素の wrangler のみで実デプロイ成立: d1 create → migrations apply --remote(drizzle フォルダ形式 = migrations_pattern)→ secret put → deploy → 疎通・cron 確認。起動 35 ms。検証デプロイは残置 = ドッグフーディングの土台(docs/notes/session-19.md §1)。Deploy to Cloudflare ボタンは公開リポジトリ前提のため Phase 2 公開時に検証)
- [ ] ~~数週間のドッグフーディング~~ **再定義(2026-08-30 H0 — ADR-0014 改訂 1)**: 完了条件(先行ゲート)から**並行の継続活動**へ。検証デプロイ(2026-08-10 — session-19)を土台に H 系列と同時に開始し、private preview(対人信頼の数名 — 公開前に受け入れてよい範囲)を経て招待制ベータまで続ける。「自分の全プロジェクトから .env を消せた」の実証は private preview の第一利用者(所有者)が担う

## Phase 2: 公開

**完了条件: SECURITY.md + 脅威モデル文書とセットで public 化**(不変。ただし 2026-08-30 H0 — ADR-0014 改訂 1 — により、公開は独立マイルストーンから **H 系列のゲート(H5 — 招待制ベータの前提条件)**へ再配置された。docs サイトは公開の必須物から外れ GA までに — 下記 H 系列の節)

- 公開前チェックリスト: ライセンス最終確認(FSL-1.1-MIT + MIT 分割)・DCO + CONTRIBUTING.md(いずれも 2026-08-12 完了 — ADR-0003 確定、LICENSE 一式 + README 導入)、テレメトリゼロの明文化、maruhi.dev 取得(**2026-09-03 取得済み確認 — maruhi.app も取得済み。割り当ては hosted-design.md §7 L1 の裁定どおり**)、商標出願(9 類・42 類)、Deploy to Cloudflare ボタンの実検証(公開リポジトリが前提。モノレポサブディレクトリ指定と D1 自動プロビジョニング — セッション 19 申し送り)、**SELF_HOSTING.md の "Updates" 節へ環境マニフェスト移行の項目を追加**(PR-M1 導入前に作成された環境の `maruhi env rotate --init-manifest` 初期化と「環境の移行 → CI の更新」の順序要件。旧サーバー × 新 CLI ではこの案内自体が通らないため、サーバー更新を先に行うことも明記 — 2026-08-18 PR #81 pullfrog レビュー。公開前の内部移行手順自体は docs/notes/session-28.md §2-2)**(2026-08-19 PR-F1 で完了 — strict 受理〔AUTH_SPEC §12-10〕後の失敗方向の説明込み。順序は後続 PR-F3b〔境界 checkpoint 同梱〕で「サーバー → CI/CLI → 移行 rotate」へ再改訂される前提を本文に注記済み — docs/notes/session-32.md §4-2)**
- 脅威モデル文書(CRYPTO_SPEC を基に「何から守り、何からは守らないか」を明文化)**位置づけの昇格(2026-08-30 H0 裁定 DD)**: 公開前チェックリスト項 → ホステッド(招待制ベータ)の前提条件。置き場 = `docs/THREAT_MODEL.md`(英語)、目次案は docs/notes/hosted-design.md §6 — 本文の起草は次セッション
- チーム共有(add_member / remove_member、エポックローテーション、要ローテーション検出。ゼロ知識運用の UX 設計込み: 未登録ユーザー招待 = CRYPTO_SPEC 未決 #9、鍵フィンガープリント確認、退職時ローテ推奨 — ADR-0014)
- ヘッドゴシップ検証(環境マニフェスト・定期チェックポイントの設計と同時 — CRYPTO_SPEC **旧**未決 #12)**設計・仕様起草済み(2026-08-18 — Wave 3 D。PR #80、docs/notes/session-27.md、CRYPTO_SPEC 0.6-draft §4.3 / §6.2 `checkpoint` / §6.6、AUTH_SPEC §16、AUDIT_SPEC 旧未決 #2 統合。仕様 PR #80 のマージ = 所有者承認)。実装は承認後の後続 PR(PR-M1 マニフェスト → M2 チェックポイント〔受理時スナップショット保存を含む〕→ M3 値スナップショット配布・検証、M4 ゴシップは並走可 — session-27 §14)。**PR-M1(環境マニフェスト)は実装済み(2026-08-18 — テストベクター → crypto → api-schema → server → CLI の全層 + 床のマニフェスト拡張 + `--init-manifest` 移行経路。実装判断は docs/notes/session-28.md)**
- GitHub Actions 同期(ここで選択的開示 = grant_server を実装)**完了(2026-08-17 — Wave 2 全完了)**: A1 = サーバー鍵基盤 + grant/revoke CLI(PR #63)、A2 = OIDC 検証 + lease エンドポイント + 監査(PR #65)+ リプレイ先着束縛(PR #67)、A3 = CLI の CI モード(`maruhi ci run` — OIDC リース + CRYPTO_SPEC §9.1 の全検証義務 + メモリ注入。リポジトリアンカーの生成 = `maruhi project anchor`)+ リポジトリ内 setup-maruhi action(`actions/setup-maruhi` — マーケットプレイス公開は public 化と同時。設計判断は docs/notes/session-25.md)
- 環境間パリティチェック(環境モデル決定が前提)
- 監査ログ UI、Web ダッシュボード(**信頼境界は ADR-0018 — 2026-08-18 裁定**: Web は鍵・平文を持たない管理画面。値・鍵操作は各人の CLI、画面は TUI → 値なし `maruhi ui` → 値ありは独立 ADR の段階導入。W0 は「誰がこの画面を見るか」から)**W0(画面設計)完了(2026-08-28 — ADR-0018 改訂 2: Web は「静的案内 + 読み取り + 失効系のみ」、トークン・招待の発行と生値は端末限定、SECURITY_REVIEW L-2 は AUTH_SPEC §6 の既定 TTL で同時解消。画面目録・gap 分析・実装分割 W1〜W3b は docs/notes/web-dashboard-design.md、裁定は docs/notes/session-39.md)**
- CLI 配布: npm / brew tap / インストールスクリプト、macOS 公証(Apple Developer Program は公開 2〜3 週前に登録)、npm provenance、チェックサム公開。**ビルド来歴(2026-09-05 — H5 のゲート)**: 全配布物に署名付き来歴(GitHub artifact attestation / npm provenance)を付け、検証手順を docs に置く(1〜2 日)。**再現可能ビルド**: Bun `compile` の決定性をスパイクで確認し(1〜2 日)、成立すれば再ビルドスクリプト + CI の二重ビルド突合(2〜3 日)を H5 に含める。不成立なら脅威モデル文書に「来歴のみ」と正直に書く。根拠 = 「運営は読めない、コードで確かめられる」の主張は復号する CLI のバイナリが公開ソースと一致することが前提(integration-options.md §3 補足 17 末尾)。**Windows 実用対応(2026-09-04 追加 — H5 と同時を目安。招待制ベータのゲートにはしない〔所有者裁定〕)**: 現状 windows-x64 は experimental(ADR-0015 決定 7 — release workflow の起動スモークのみ。`run` の Windows 固有処理〔PATHEXT / COMSPEC / 環境変数名の大小非区別〕は実装済み)。実用化 = Credential Manager 経路(`Bun.secrets`)の実機検証・CI テストマトリクスへ windows を追加・`run` の引用符 / パスの端の確認・scoop または winget・WSL の案内(linux 版は動くが Secret Service 不在で login が fail-closed になる可能性 — 要検証)。概算 1〜2 週(大半は検証)。対象層(Vercel に置く Web 開発者)に Windows + WSL が一定数いるため入口寄りの項目。**2026-09-04 の推奨: 検証は安価なので SY 系列と並走で早めに着手し、ゲートにはしない**(ベータの案内では「experimental」表記を外せる状態を目指す)。**関連(2026-09-04 発見 — integration-options.md §3 補足 8)**: Codespaces / devcontainer / WSL / 素の Linux サーバーは **OS キーチェーン不在クラス**で、現状は `login` と鍵保存が型付きエラーで止まる(平文フォールバックなし = 正しい)。「キーチェーン不在環境で人間が使う経路」(Secret Service 導入案内 / 端末セッション限定のメモリ保持 / パスキー PRF 封印 — device-key-sealing.md)を Windows 実用対応と同時に独立項目として裁定する
- 運用側デプロイを Alchemy v2 へ載せる(ADR-0012。現状は素の wrangler 経路のみが実在 — セルフホスト配布物はこのまま維持する)。spike-b / セッション 03 の申し送り: ① state store が常設 worker + secret を張る設計を運用として受け入れるか `state:` を差し替えるか ② worker 名が Alchemy の命名規則(`<stack>-<resource>-<stage>-<hash>`)になるので stage / 命名の明示設定 ③ ソースを Alchemy 非依存に保つため **Async Worker 形式に固定**(Effect ネイティブな Worker/DO 記述を使うと wrangler 経路が壊れる)
- ユーザーに見える文言を英語へ統一(ADR-0017)。**CLI 分は完了**(ADR-0016 第 3 段階 — コマンド単位の移行 PR + 最終コミットの共有モジュール一括英語化で、`pull` / `run` / `env create` の先行 3 コマンド分も解消済み)。**web / README / インストーラ / 配布物ドキュメント分は完了**(web = `Root.tsx` の `lang="en"`。README / CONTRIBUTING / SELF_HOSTING / install.sh のユーザー可視メッセージ)。docs サイト(Blume — `apps/site`、DP2 で構築)も英語で書いている(LP / docs の文言・`blume.config.ts` の banner を含む)
- docs サイト(Blume)**後回し(2026-08-30 オーナー裁定 — H0)**: 公開の必須物から外す(公開時は README + SELF_HOSTING で足りる)。GA までに構築(H 系列の節を参照)

## ホステッドクラウド版(H 系列 — 2026-08-30 H0 で実スケジュール化。ADR-0014 改訂 1)

**完了条件(招待制ベータ開放 = H6): S1〜S3 着地 × H1〜H5 完了 × DP1〜DP5 完了 × SY1〜SY5 完了 × KL3 完了〔保護者つきリカバリー + 端末移行のハンドオフ — 2026-09-05 所有者裁定でゲートに追加〕× KF 完了 × IV1〜IV2 完了〔招待儀式の軽量化 — 同日裁定でゲートに追加〕× public 化**(**2026-09-04 所有者裁定で SY 系列〔同期先 — Secret Syncs〕をゲートに追加** — 対象層は Vercel / Cloudflare / Netlify にデプロイする個人・小チームで、「Vercel にはどう入れるの?」に「手で貼ってください」と答えると初日に離脱する入口のゲート。競合 5 社は無料枠で同期を提供 — docs/notes/competitive-analysis.md §4、設計案は docs/notes/integration-options.md §3。**2026-09-03 所有者裁定で DP 系列〔デザインパス — Design Pass。D1 等の Cloudflare D1 との字面衝突を避けて DP〕をゲートに追加** — 「ここがないと誰も使ってくれない」。W 系列は機能として画面を揃えたのみで、デザインの通し作業は未着手)。設計・gap 分析・実装分割の正は docs/notes/hosted-design.md、裁定録は docs/notes/session-47.md(CY〜DE)。ADR-0009 の再判断は実施済み(GitHub 直実装のまま続行 — 同 ADR の再判断記録)。

順序(ADR-0014 改訂 1 + 2026-09-03 追記): ドッグフーディング(private preview 込み — 並行・継続)/ S1〜S3 と H1〜H3 と **DP1〜DP5** と **SY1〜SY5** の並走 → **SY1〜SY5 着地 → H4 法務**(2026-09-04 所有者裁定: 同期先は法務の前)→ H5 公開儀式 → public 化 → 招待制ベータ → オープンベータ → GA(課金・docs サイト)。DP 系列は H4 の静的ページ(法務文書)・H6 の web 静的面(ベータ案内・waitlist・拒否時の着地)の**見た目の受け皿**でもあるため、DP1 / DP2 を先に固める。

- [x] H1: サインアップ制御 — deployment 設定 `signupPolicy`(open / invite / closed。セルフホスト既定 open = 現行挙動)+ サインアップ招待コード(単回・期限つき bearer — **受理・事前検証は Web サインアップ〔AUTH_SPEC §3〕のみ: 2026-08-31 追補裁定 DH で CLI ログインはアカウントを作らなくなったため、CLI 経路にコードは載らない**)+ `/auth/config` の advisory 配布 + プロジェクト招待(AUTH_SPEC §15)との合成経路 + **CLI の login 事前 fail-fast(`POST /auth/cli/start` 前 — signupPolicy 確認と「Web でサインアップしてから」の案内に縮小。hosted-design.md §2-2 再追記)**。AUTH_SPEC §3 / §4 の改訂は実装 PR 側(§4 の本体改訂は 2026-08-31 に着地済み — H1 分は signupPolicy まわりの追補のみ)**(2026-09-01 実装完了 — PR #133。仕様 = AUTH_SPEC 0.18-draft §3 / AUDIT_SPEC 1.4-draft §3.1〔`auth.signup_denied`〕。事前検証は独立エンドポイントを設けず start ハンドラに畳む・コード消費はアカウント作成と同一 D1 トランザクションの CAS・未知の設定値は closed 扱い〔fail-closed〕— 実装裁定は PR 本文)**
- [x] H1b(2026-08-31 追加 — session-48 裁定 DF + 追補裁定 DH): CLI ログイン経路の置換(gap 9 の実装 — AUTH_SPEC §4 全面改訂 2026-08-31): サーバー `POST /auth/cli/start`(無記録 — 署名付きフロー資格)/ `GET /auth/cli/verify` / `POST /auth/cli/poll` + スクリプトなし承認ページ + サインアップ案内ページ(既存アカウント限定)+ 旧 `/auth/device/exchange`・check-token・形式事前検査の削除、CLI = login の置換(device flow 削除・client_id 解決の廃止・プロバイダ非依存)。既存 `tokenName` Schema の文字種制約(AUTH_SPEC §6 追記・非遡及)と `docs/SELF_HOSTING.md` の追随込み。H1 と独立・並走可。**H5 の前提に加える**(公開後の削除は破壊的変更 — 公開前が最安。hosted-design.md §9 追記)**(2026-08-31 実装完了 — PR #117。承認ページの CSP `frame-ancestors 'none'`・CLI 側 opener の URL 検証などレビュー対応込み。旧エンドポイントは猶予窓なしの即削除 — 公開前につき §4-3 の委任判断)**
- [x] H2: テナント quota — プロジェクト数 / org 上限(起草値 アクティブ 100)+ DO ストレージ総量ガード(AUTH_SPEC §12-8 の Phase 2 予告の実装 — 警告 / 拒否の 2 段)**(2026-09-02 実装完了 — PR #134。仕様 = AUTH_SPEC 0.19-draft §11-3 / §12-8。実装裁定: 上限到達時も DO へ report-only〔`admitFresh: false`〕で問い合わせ §11-3 の修復経路を塞がない・429 `ProjectLimit`・DO ガードは `databaseSize` 毎受理測定で 422 `DataLimitExceeded`〔`project-storage-bytes`〕、読み取り / 削除 / 失効 / ローテーション / リース / 申告 / checkpoint は拒否下でも受理〔監査ヘッド派生列の実体化を要する読み取りのみ例外〕・警告は静的ログ 1 回 / DO インスタンス — 詳細は PR 本文)**
- [x] H3: 運用基盤 — 監視・アラート(D1 総量・GitHub クォータトリップワイヤ・外形監視)、バックアップ(D1 Time Travel + 定期 export、DO のアプリレベル退避の設計)、リストア演習、運用 OAuth App。(任意)運用側デプロイの Alchemy v2 化(ADR-0012 — 既存項の再掲)**(2026-09-02 実装完了 — PR #137。**2026-09-03 リストア演習〔hosted-ops.md §5-3 / O7〕を実機で実施し突合一致 — H3 完了**。hosted origin `https://my.maruhi.app`。演習で Effect HTTP ロガーによる capability のログ残留を発見・修正、runbook 3 点を実機に合わせて訂正。設計 = docs/notes/hosted-ops.md。実装裁定: 計数は D1 固定窓カウンタ〔GitHub token 請求 = exchangeCode の装飾・フロー上限到達〕+ 既存監査行の窓集計〔サインアップ拒否 / 抑制マーカー〕、判定 = worker の毎時 cron、通知 = 運営 webhook〔Secret 未設定は無効・静的信号名 + 集計値のみ〕、D1 総量 = export ワークフローの `wrangler d1 info`、H2 警告行 = 退避スイープの census で件数化、DO → R2 = DO 自身が permit 下で NDJSON gzip を multipart〔完全 + skip 規則・キーは DO id の像〕、復元 = 空 DO のみの内部 RPC + HTTP なしの一時デプロイ復元 worker〔R2 ジョブファイル・drill 名前空間〕、hosted 固有バインディング = wrangler 名前付き環境 `hosted`〔セルフホスト既定は無変更〕、D1 export = GitHub Actions cron + age。**人間タスク**(hosted-ops.md §7): O1〜O7 は 2026-09-03 完了〔O5 = Better Stack Uptime〕、O8 退避物の追加暗号化は 2026-09-03 所有者裁定で不要〔恒久 — hosted-ops.md §7〕・O9 Alchemy v2 化は独立 PR — **DP 系列の後**〔web-design-pass.md §1-6〕)**
- [ ] **DP 系列: デザインパス(2026-09-03 追加 — 招待制ベータのゲート)**。**裁定 = docs/notes/web-design-pass.md(2026-09-03 所有者裁定済み: ㊙ の自前 SVG ロゴ + 朱 accent〔彩度は落とさない〕・システム追従ダーク〔Astryx 既定に寄せる〕・ダッシュボードはシステムフォント〔等幅の自己配信は実機確認後〕・LP / docs は自己配信の Archivo + Martian Mono〔OFL — bun.com と同じ 2 書体〕・LP は apex に独立静的サイトで docs は `maruhi.app/docs`・トラッカーなし〔CF Web Analytics のサーバー側集計のみ〕・O9 は DP の後)**。前提規律: ADR-0013(見た目は defineTheme → xstyle → ui.package の順。swizzle 禁止・`className` 禁止)・ADR-0018(Web は鍵・平文を持たない)・TCB 規則(ダッシュボードのオリジン `my.maruhi.app` にサードパーティスクリプト / CDN / 外部フォント / アナリティクスを載せない)。**着手前に短い設計セッション**(所有者の好み: 参考製品・トーン・配色・ダーク優先か・LP を別サイトにするか)を置き、裁定を `docs/notes/web-design-pass.md` に残してから PR 分割する
  - [x] DP1: ブランド基盤 — `apps/web/theme/maruhi.ts` の仮ブランド(accent `#C73E3A`「スパイク用」)を確定ブランドへ(朱の accent seed〔SVG ロゴの赤と一致〕・neutral warm 維持・typography / radius は Astryx 既定・`[light, dark]` タプルで両モード)、**㊙ の自前 SVG ロゴ**(OFL の CJK 字形をパス化。絵文字は使わない — 描画のベンダー依存とライセンス)、favicon / OG 画像、danger と accent の色相分離の確認。成果物は defineTheme と静的アセットのみ(ADR-0013 ①)**(2026-09-03 実装完了 — 朱 = `#C1330B`(light)/ `#FF693C`(dark)、HCT hue 44 で danger(hue 28)と 16° 分離。seed タプル + `tokens` 2 件〔`--color-accent` / `--color-on-accent`〕の併用 — Astryx の seed 導出は dark accent をトーン 80 の pastel に固定するため。ロゴ = Noto Sans CJK JP Bold の「秘」をパス化した SVG 4 点〔accent / 反転 / モノクロ / favicon〕+ PNG〔favicon 32 / 192・apple-touch-icon・OG 1200×630〕、OFL 全文は `apps/web/public/fonts/`。裁定録 = web-design-pass.md §3。朱の最終 hex は所有者確認待ちの唯一の点)**
  - [x] DP2: LP(S1)の本実装 + apex 配置(hosted-design.md §7 **L1 と一体**)**(2026-09-03 実装完了 — `apps/site`〔Blume 1.5.3・`apps/docs` スタブを吸収〕: LP `/` = Blume カスタムページ〔PageLayout + scoped CSS〕・docs `/docs/*` = Blume 既定・theme tokens は `apps/web/theme/maruhi.css` から生成〔裁定 B〕・Archivo / Martian Mono を Fontsource の Latin サブセット woff2 で自己配信 + OFL 同梱〔裁定 C〕・CSP は unsafe-inline なし〔inline script と Astro Fonts の style 要素はビルド時ハッシュ、Shiki の style 属性はクラスへ外部化 — 裁定 D〕・独立 wrangler 設定 `maruhi-site`〔裁定 E〕・`my.maruhi.app/` は最小案内 + `/about` に診断フック〔裁定 F〕。裁定録 = web-design-pass.md §4。**人間タスク = hosted-ops.md §7 O10〜O12**〔初回デプロイ・`maruhi.dev` 301・訪問数集計〕。waitlist フォームは H6)** — 2026-09-03 裁定で製品オリジン = `my.maruhi.app`・apex `maruhi.app` = LP + docs(`/docs`)・`maruhi.dev` は 301。**LP / docs は独立の wrangler 設定(Workers Static Assets)で立て、後日 O9(Alchemy v2 化)で宣言に包める形にする**。現 `HomePage.tsx` はスパイクの骨格(カウンター・built-at・about は e2e 検証フック)。「最初の 5 分」(ADR-0014 改訂 1)へ導く構成 = 価値提案 → インストール → `maruhi login` → 招待制の案内 / waitlist。**裁定済み**: 別の静的サイト(Blume — ADR-0008。既存 `apps/docs` スタブを吸収可)。外部スクリプト・外部フォント・トラッカーは置かない(「言わざる」を LP にも適用 — 訴求点にする)。訪問数は Cloudflare Web Analytics のサーバー側集計のみ。**LP の文言・比較表の方針(2026-09-04 追記): 訴求点 6 項・星取表を置かず信頼モデル 3 型表にする方針・英語コピー案・表示規律は docs/notes/competitive-analysis.md §6-1 — LP の文言改訂時に参照**
  - [x] DP3: ダッシュボードのデザインパス(W 系列〔web-dashboard-design.md §7 — W3b で完結〕の後続としての見た目の通し作業。W 系列の番号は増やさない) — アプリシェル(ヘッダー・ユーザー・ログアウト・プロジェクト間ナビ・パンくず)、画面ごとの情報設計(空状態・ローディング・エラー = 13 か所から呼ばれる `FailureNotice` の統一、監査ビューアの可読性〔表示規律 web-dashboard-design.md §4 を保つ〕)、レスポンシブ(HP5 = モバイルで監査を見る利害関係者)、a11y 監査(Astryx = React Aria 基盤)。同じ xstyle 上書きが 2〜3 回出た箇所は人間に提案してから `ui.package` へ昇格 **(2026-09-04 実装完了・PR #148 — `DashboardShell`〔Astryx テンプレート `shell-side-nav` / `login` の形 — AppShell + SideNav: ロゴ・Projects(現在のプロジェクトを子項目に)/ API tokens / Account audit・フッターにユーザー id と Sign out・LayoutHeader にパンくず + h1。セッション状態をシェルが 1 か所で持ち 401 は全画面で同じサインイン画面。所有者レビューで TopNav → SideNav に改訂〕、状態の規律〔`FailureNotice` 置換 / 追記の 2 形 + `EmptyNotice` + `LoadingRow`〕、監査ビューアは 1 列の行 + その場で展開〔`Collapsible` — 幅によらず同じ形、HP5〕、サーバー時刻は `Timestamp`、集合の容器は `Card`〔`SectionBlock`〕、境界は線でなく余白、`HexText`〔識別子の折り返し — ui.package / variant への昇格候補として人間に提案〕、a11y〔axe-core 24 態で違反 0・空見出し列に名前・SegmentedControl → ToggleButtonGroup〕、`test/screenshots.ts`〔配信物を変えないスクリーンショット手順〕。裁定録 = web-design-pass.md §5)**
  - [x] DP4: 認証・儀式ページ(サーバー配信のスクリプトなし HTML) — CLI 承認ページ(確認コードの視認性 = フィッシングガードの UX)、サインアップ案内・拒否時の着地(H6 の明示項目)、`/invite` 受諾ページ。CSP `script-src 'none'` のまま、自己配信 CSS でブランドを統一 **(2026-09-05 実装完了・PR #150 — 共有スタイル `apps/web/public/pages.css`〔/invite とサーバー配信ページの共用。色 / フォント / 角丸は `var()` だけ〕+ ブランドの正の生成物 `theme/maruhi.css` を無変換で `/theme.css` として同梱〔`write-headers.ts` がバイト等価を検査。生成器も写しも無し〕、CSP は `script-src 'none'` のまま `style-src` / `img-src` を `'self'` に〔meta とヘッダーの両方。inline のハッシュ許可なし〕、共通枠 `page()`〔ブランドヘッダー = `logo-inverted.svg` + ワードマーク、h1 = ページの題〕、承認ページの確認コードを最大要素に〔システム等幅 + 字間 + slashed-zero。Crockford 32 字の文字集合を確認、自己配信フォントは不要と判定〕・付与内容を `dl`・Approve = accent 塗り / Deny = outline、案内 / 拒否ページを「何が起きたか → 何が起きていないか(outcome 行: No account was created. 等)→ 次にできること」の 3 段に統一〔一様性は不変〕、`/invite` を同じ枠へ〔per-path CSP・機械検査・301 は不変。`pre` の到達性を修正〕、静的アセットの既定〔max-age=0, must-revalidate + ETag〕により CSS の URL 版数は不要と裁定し e2e で固定、axe 44 態で違反 0。裁定録 = web-design-pass.md §5)**
  - [x] DP5: CLI の UX — 出力の一貫性(色・記号の TTY 規律)、`login` のフロー期限(10 分)と案内文、繰り返し出る Note(push ごとの anchor 注記・初回からの checkpoint 警告)の抑制規則、エラー文の英語校正、`--help` の整合。ADR-0016 決定 7(エージェント環境の値表示拒否)の文言も含む **(2026-09-05 実装完了 — PR #__PR__。通知は `notice.ts` に集約〔`Note:` / `Warning:` / `maruhi:` の 3 語・宛先はすべて stderr・色は接頭辞だけ〔シアン / 黄 / 赤〕・記号なし・判定は `CliIo.colorEnabled()` = stderr が端末か + NO_COLOR / FORCE_COLOR / TERM=dumb〕。Bun の `console.error` が stderr 全体を赤で塗っていたのを `writeSync` で解消。抑制規則 = 未発行の checkpoint 基準は genesis から 7 日で数える〔境界チェックポイントは公証なしのため admin には作成当日から提案が出ていた〕+ push のアンカー助言は同じ 1 行に同梱 + 同一文面の Note / Warning は 1 実行 1 回。`login` の案内は stderr〔結果は stdout〕・期限はサーバー応答の `expiresInSeconds` から導く〔**フロー期限は 10 分ではなく 15 分** — `CLI_FLOW_TTL_MS`。CLI に定数は持たない〕・語彙は承認ページ〔DP4〕と統一。英語の用語集と `--help` の golden(`apps/cli/test/golden/help.txt`)。裁定録 = web-design-pass.md §5)**
- [ ] **SY 系列: 同期先(Secret Syncs — 2026-09-04 追加。招待制ベータのゲートは SY1〜SY5 で、H4〔法務〕より先に着地させる。SY6〔変更通知〕は需要駆動へ降格 — 2026-09-05)**。番号対応: 設計メモ §3 の案番号(S1〜S13 — メモ内の通し番号で、値なしスキーマの S 系列とは無関係)と ROADMAP の SY 番号は別物。SY1 = 案 S1、SY2 = 案 S2 + S8、SY3 = 案 S3、SY4 / SY5 = 案 S2 のプリセット追加(+ GitHub は案 S6 の部品)、**SY6 = 案 S4(変更通知)**。案の列挙と評価は docs/notes/integration-options.md §3(競合の現状は docs/notes/competitive-analysis.md §2-4)。原則: **平文は各人の機械か CI ランナーにしか置かない**(運営のサーバーが復号して push する形 = Phase の SSE 型は**ホステッドに出さない**。セルフホスト限定の後続 — 同メモ §8 項 1)。同期は一方通行(maruhi が正。同期先からの取り込み・双方向は ADR-0014 ガードレール)
  - [ ] SY1: レシピ(docs のみ) — `maruhi run -- wrangler secret bulk`(Cloudflare Workers)、`vercel env add` 等、既存コマンドの組み合わせを README / SELF_HOSTING に置く(概算 0.5 日)
  - [ ] SY2: `maruhi sync <target>`(CLI 主導・手動) — **作り = ドライバ 1 インターフェース × 2 種類(2026-09-04 第 5 ラウンド — integration-options.md §3 補足 10 / 13)**: **手元では導入済み・ログイン済みのベンダー CLI(`vercel` / `wrangler` / `gh`)を `exec` ドライバで先に使う — トークン作成が不要になり、競合の OAuth 連携との「1 手間」が消える(2026-09-05 補足 16)**。`npx` で取りに行かない・自動インストールなし・ベンダーテレメトリ off〔`VERCEL_TELEMETRY_DISABLED` / `WRANGLER_SEND_METRICS` 等〕・ログ抑止を共通部品で付与。**CI とベンダー CLI 未導入時は `http` ドライバ**(第一級 = Vercel / Cloudflare Workers。maruhi 自身のコード・一括 upsert・型付きエラー。統合トークンは maruhi の変数として E2EE・メモリで取り出す)。ロングテールは exec を基本に stdin 不可なら http。**設定の置き場(第 7 ラウンド — 補足 15)**: 対応付け(maruhi 環境 → 同期先 / project id / 環境)は非機密設定としてリポジトリにコミット(アンカーファイルと同じ扱い)、統合トークンは設定で指した任意の環境の普通の変数。特別な環境名は持たない。**同期レシート**: 「どの先にどの変数の version まで届いたか」を設定で指した環境の変数(E2EE + 署名)に保存し、`sync diff` は同期先を読み戻さずに済ませ、未同期の印を自然に出す(値由来のダイジェストは使わない)。レシートはローテーション工程 ④ の追跡器を兼ねる(需要駆動節)。**エポックローテーションは現在値を新 version として再暗号化するため(CRYPTO_SPEC §7)、`env rotate` の実行者がレシートを新 version へ進める**(誤った全件再同期を防ぐ — 補足 14 M1)。プリセットは http / exec とも宣言的(JSON + モック応答)。`sync plan` / `sync apply`(production は既定で plan のみ)。環境の対応付け(maruhi 環境 → 同期先の環境)。**第一級は Vercel / Cloudflare Workers の 2 つ(2026-09-05 所有者裁定)。Netlify は SY4 の第 2 波(http)**。production 同期を CI で行う場合は GitHub Environments の required reviewers で四眼が得られる(docs パターン — 補足 15 X4)。**同期の最終形の一覧は integration-options.md §3 冒頭「同期の最終形」**。**設計の推奨(2026-09-04 第 3 ラウンド — 所有者裁定待ち。integration-options.md §3 補足 4 / 7 / 9)**: (a) **エージェント環境は `run` と同じ扱い**(専用ゲートなし。`run` は既にエージェント環境で許可されており、同期は新しい持ち出し経路ではない — 補足 9)。値を stdout に出さない・監査行 `sync.executed`(プリセット id・環境のみ)・自作プリセットの**作成**だけ TTY(誤操作ガード)。(b) 同期設定(プリセット id・同期先 id・自作プリセット)は maruhi の変数として保存(E2EE + 署名。チーム内の一貫性のため。仕様改訂なし)。資格を絞りたいチームは**別プロジェクト**に置く(メンバーシップ単位 = プロジェクト — 補足 7 P2)。(c) **push 時同期**: 設定があれば `maruhi push` 直後に書き手の CLI が直接同期するか、`gh workflow run` で CI を起動する(書き手に同期先トークンを持たせない形 — 補足 7 P1)。環境ごとの `autoSync`(production は既定で手動)。失敗は「未同期」印で次回 / CI が回収。仕様改訂なし(概算 5〜8 日)
  - [ ] SY3: CI から同期 — `maruhi ci run -- maruhi sync <target>` の workflow テンプレートと docs(既存の OIDC リース・リポジトリアンカー検証に乗る。`grant_server` 済みが前提 — サーバーは DEK を持てるが復号しない経路)。**2 つの標準形**(補足 7): ① CI がデプロイを握る対象(Cloudflare Workers / Fly / Railway / AWS / GCP)は**デプロイ時再適用**(デプロイ workflow 内で毎回 `maruhi ci run -- wrangler secret bulk` 等 — ドリフトが構造的に消え、通知も突合も不要)、② Git 連携で相手がデプロイする対象(Vercel / Netlify)は書き手からの CI 起動(SY2 (c))+ `schedule` の定期突合(`sync diff` で一方向上書き・同期先の手編集は差分報告)。仕様改訂なし(概算 1〜2 日)
  - [ ] SY4: 第 2 陣 — **Netlify(http)**、Railway / Render / Fly.io / Deno Deploy / Supabase(Edge Functions)/ Cloudflare Pages(exec を基本、stdin 不可なら http。Expo EAS は需要次第)。SY2 の汎用アダプタへのプリセット追加(各 0.5〜1 日。GraphQL の Railway / Fly.io は少し多め)。概算 4〜6 日(**2026-09-04 所有者裁定でベータ前に前倒し**)
  - [ ] SY5: GitHub Actions secrets — 第 1 段 = レシピ `maruhi run -- sh -c 'printenv X | gh secret set X'`(`gh` が封印を行う。暗号依存の追加なし・即日。Dependabot / Codespaces も `gh secret set --app`)。第 2 段 = **CI 上で `gh secret set`**(GitHub のランナーに `gh` 同梱。SY3 の workflow 内で自動化。`GITHUB_TOKEN` は secrets を書けないため `secrets: write` の fine-grained PAT か App トークンを bootstrap として置く)— **依存追加なしで手動 + 自動が揃うので、ネイティブ封印(libsodium 系の依存追加 = CLAUDE.md の暗号規律への例外)は不要と判断(2026-09-04 再探索 — 補足 5。所有者裁定待ち)**。`gh` を入れられない環境の声が出たら `@noble/*` を同期プリセットのモジュールに隔離して再検討。bootstrap の PAT は後から `actions/create-github-app-token`(App の短命トークン)へ差し替え可(補足 8 Q1)。**目標は「GitHub secrets を空にする」**(Actions はリースで maruhi トークンすら置かない。残るのは既製アクションの入力・Dependabot・`secrets: inherit` のみ — 既製アクションは「ベンダー CLI を `maruhi ci run` で直接叩く」への置き換えを案内 — 補足 8 Q2 / Q3)。概算 1〜2 日(**同裁定でベータ前**)
  - SY6(= 設計メモの案 S4。**需要駆動へ降格 — 2026-09-05 所有者裁定〔同期の最終形の承認に含む〕**。理由: push 時同期・書き手からの CI 起動〔SY2 (c)〕+ デプロイ時再適用 / 定期突合〔SY3〕が上位互換で、webhook が要るのは「`gh` がなく・CI からしか同期せず・即時性が要る」の三重条件のみ — 補足 4 / 7): 変更通知 webhook(「呼び鈴であって配達ではない」)— 受理後に「プロジェクト × 環境が更新された」だけをユーザー設定の送信先へ送り、SY3 を即時化する。送信先は初期は既知先(GitHub `repository_dispatch` 等)に限定(SSRF・流出面 — 同メモ §8 項 2)。値・変数名・project_id(capability — AUTH_SPEC §11-2)を載せない。AUTH_SPEC / AUDIT_SPEC の追記が先(概算 4〜6 日)。変更通知はローテーション・SDK のキャッシュ無効化にも使う共通部品(同メモ §7)。**送信経路(2026-09-04 推奨 — 所有者裁定待ち)**: 両方持つ。基本形 = 汎用の送信先(URL + 静的ヘッダ + HMAC 署名 + 既知ホストの allowlist)を先に作る — Vercel / Netlify の deploy hook(認証なしの秘匿 URL)・GitLab の pipeline trigger・GitHub の fine-grained PAT がこれだけで動き、セルフホストでも App 登録が要らない。GitHub App(インストールトークンの都度発行)はその上の「認証モード」として後から足す — ホステッドでユーザーの PAT を預からずに済む形(同メモ §3 補足 2)
  - 第 3 陣以降(需要が見えてから — 同メモ §3 補足 1): AWS SM / GCP SM / Azure KV(専用の認証コード。SigV4 はローテーションのコネクタと共用)、GitLab CI / CircleCI、K8s。プリセットは寄贈を受けやすい形(スキーマ + モック応答)にする
  - **同期は無料枠に置く(2026-09-04 所有者決定)**: SY1〜SY5 はサーバーコストゼロ(変更通知 SY6 を入れる場合だけ変更 1 回に送信 1 回)。競合の有料化は価値ベースの線引きであり原価ではない(同メモ §3 補足 3)。課金の線は org のメンバー数と Enterprise 枠
  - 後続候補(需要が見えてから): GitHub secrets の sealed box 中継 = サーバーが読めない自動同期(同メモ S6)、ユーザー所有の同期エージェント = 受信者の一般化で CRYPTO_SPEC 改訂(S7)、セルフホスト限定のサーバー復号 push(S5)
- [ ] **KL: キーチェーン不在環境と鍵のハンドオフ(Codespaces / devcontainer / WSL / 素の Linux — 2026-09-04 追加。2026-09-05 所有者裁定: KL3〔保護者つきリカバリー + 端末移行〕は招待制ベータのゲート。KL1 / KL2 は並走)**。案の列挙と推奨は integration-options.md §3 補足 12。順序: KL1 = Secret Service 導入レシピ(gnome-keyring + D-Bus。docs のみ・即日)→ KL2 = セッション内メモリ保持(`maruhi agent` — ssh-agent 型。ディスクに書かない)+ 既存のリカバリーラップで同一鍵を復元する暫定経路(仕様変更ゼロ)→ KL3 = **封印バックアップを CRYPTO_SPEC §8 の第 2 経路として一般化**(KEK = パスキー PRF 由来〔高エントロピー → HKDF で足りる・ストレッチング不要〕。PRF は CLI が配る localhost ページで取得 — ADR-0018 と整合。パスフレーズ由来は §8 の Argon2id 要件と WebCrypto 規律の二重衝突があるため採らない)。KL3 は「将来」節のリカバリー封印バックアップ・パスキー PRF と同一機構であり、端末移行の儀式も同時に軽くなる。**KL3 の設計は「master 鍵ラップ台帳」の一般化として、受信者 = リカバリーコード / パスキー PRF / 保護者(指名したチームメイトの公開鍵 — **1-of-n〔誰か 1 人〕と n-of-n〔KEK を XOR 分割して全員が必要〕を選べる**。鍵とコードを両方失っても保護者 + 本人の GitHub ログインで復元)/ 自分の旧端末(端末移行)を一度に扱う(2026-09-05 所有者裁定: **保護者つきリカバリーを採用し、KL3 として招待制ベータ前に実装する(ゲート)**。概算 3 週 — 仕様 2〜3 日〔CRYPTO_SPEC §8 一般化 + AUTH_SPEC §13 拡張 + AUDIT 事件〕→ crypto + テストベクター 2〜3 日〔人間レビュー〕→ サーバー 3〜4 日 → CLI 4〜5 日 → docs 1 日。設計要点は integration-options.md §3 補足 17)**。ミラー(自分の CF に読み取りレプリカ)と鍵ログイン(メンバー鍵でセッション)は後回し、QR 儀式はやらない(同補足)
- [ ] **KF: 検証済み指紋帳(2026-09-05 追加 — SY / KL と並走・ベータ前。概算 1〜2 日)**: 鍵はユーザー単位なので、一度 12 語で確認した相手の指紋を CLI が非機密設定として保持し(SSH の known_hosts と同じ発想)、同じ相手を別プロジェクトへ招くときは儀式を自動で通す。「初回と招待だけ」の儀式が「相手ごとに初回だけ」に縮む。暗号変更なし。設計は integration-options.md §3 補足 17 末尾(第 A 巡)。**儀式そのものは据え置き**(KT で省略できるという前段の記述は訂正済み — 儀式が守るのは `add_member` から被招待者の初回同期までの窓であり、KT はその窓を短くしない)
- [ ] **IV: 招待儀式の軽量化(2026-09-05 追加。同日所有者裁定: 招待制ベータのゲート。KL3 と同じ仕様改訂サイクル)**: IV1 = 招待リンクの秘密を `redeem` / `bind` に分け(HKDF)、受諾に `HMAC(bind, 受諾鍵)` を添える。`bind` はサーバーに渡らないため**サーバーによる鍵すり替えが暗号的に不可能**になり、12 語の照合はサーバー脅威に対して不要になる(CRYPTO_SPEC §6.5 + AUTH_SPEC §15 改訂・テストベクター・crypto レビュー。概算 1〜2 週。受諾ブロックのワイヤ形式が変わるため外部ユーザーの存在前が最も安い)。IV2 = GitHub の SSH 署名鍵ディレクトリ(公開 API)で受諾鍵を照合(`maruhi key publish` で登録。リンク盗難にも効く。CLI が github.com へ問い合わせる初例 — 所有者裁定。概算 1 週)。両方が通れば招待は「リンクを渡す → accept → add」で完了し、12 語は失敗時のフォールバックになる。**役割分担**: IV1 が本体(サーバー脅威を暗号で消す。IdP に依存しない)、IV2 は「リンク盗難」の残余を閉じて**儀式なしを既定にするための条件**(IV1 だけなら残余は Bob 側の受諾衝突〔410〕で検出可能だが事前防止ではない)。**IV2 は「身元の裏付け元」を差し替え可能な抽象にする**(`github-signing-keys` / 将来の `org-directory`〔組織の鍵台帳 — SSO 導入時の同等物〕/ `none` = 儀式)。SSO(ADR-0009 の WorkOS 挿入点)を後で入れても IV1 は無変更、IV2 は裏付け元を org 台帳に切り替えるだけで、チェーン・鍵・招待の形は変わらない(チェーンにプロバイダ ID を書かない規律のとおり)。設計は integration-options.md §3 補足 18
- [ ] H4: 法務・商務 — ToS / プライバシーポリシー / AUP、サブプロセッサ整理、security@ 窓口、ステータスページ、アカウント削除(gap 6)・通知経路(gap 8)の設計 + **法務文書の配信面(web の未認証静的ページ — hosted-design.md §9「web 面の帰属」)**(人間タスクの列挙は hosted-design.md §7)
- [ ] H5: 公開儀式 — SECURITY.md + 脅威モデル文書(`docs/THREAT_MODEL.md` — 起草計画は hosted-design.md §6)+ 公開前チェックリスト残(Deploy to Cloudflare ボタン検証・CLI 配布・macOS 公証・**ビルド来歴〔ゲート〕・再現可能ビルドのスパイク**・Windows 実用対応〔Phase 2 の CLI 配布項 — ゲート外・目安〕)→ **public 化**
- [ ] H6: 招待制ベータ開放 — 「最初の 5 分」体験要件(ADR-0014 改訂 1)の充足確認・招待コード発行運用 + **ベータ案内・waitlist 導線・サインアップ拒否時の着地文言・オンボーディングの web 静的面(S1 系の拡張 — hosted-design.md §9)**
- [ ] オープンベータ(H6 の後): quota 実測 + GitHub クォータ余裕(2026-08-31 更新: 律速は device flow 50 回/時から token 請求 2,000 回/時の自前計数へ — hosted-design.md §3-4 追記)+ ~~CLI ログインのスケール経路(gap 9)の裁定~~(2026-08-31 裁定済み — session-48 裁定 DF。実装 = H1b)を経て開放(SY6 変更通知は需要駆動へ降格したためゲートから外す — 2026-09-05)
- [ ] エクスポート / セルフホスト移行(export / import)— **実施決定・時期未定(2026-08-30 オーナー決定)。ベータのゲートにしない**: ホステッドから自分のデプロイへ、チェーン・値・マニフェストを履歴ごと運ぶ経路。構造的には可搬(project_id = genesis ハッシュ・自己検証チェーン・暗号文)で欠けているのは輸送路のみ。受理面に触れるため独立の設計セッション(設計 → 承認 → 実装)。論点と非可搬部分(`grant_server` ラップの再 grant 等)は docs/notes/hosted-design.md §10
- [ ] GA: 課金導入(設計は独立タスク — 未起草)・docs サイト(Blume — Wave 3 G)。**価格表に問い合わせ限定の Enterprise 枠を置く(2026-09-04 所有者方針)**: SSO / SCIM・監査ストリーミング・セルフホスト向け商用サポート等を「問い合わせ」に置き、需要の計測装置にする。実装は問い合わせが来てから(SSO は ADR-0009 の WorkOS 挿入点。下記「需要駆動」節)

## Phase 3: エージェント(方針は ADR-0014。優先度順)

- 値なしスキーマ(名前・型・説明・必須のみをエージェントへ開示。配信は `maruhi schema` CLI — MCP 配信は後回し〔次行〕。`.env.schema` ファイルは正にしない)— **仕様起草済み(2026-08-30 S0)**: CRYPTO_SPEC 0.8-draft §4.2 レイアウト v2 / AUTH_SPEC 0.16-draft §12-11 スキーマポリシー。CLI・付帯面と実装分割(S1〜)は docs/notes/value-free-schema-design.md、裁定は docs/notes/session-46.md(検討メモ: docs/notes/value-free-schema.md)。**S1 は 2026-08-31 実装完了(PR #116 — テストベクター先行 + `packages/crypto` のレイアウト v2 encode / verify・declared 対応。v1 ベクターは純追記で不変。CI に参照検証器を常設)**。**S2 は 2026-09-01 実装完了(PR #119 — `packages/api-schema` のワイヤ v2〔layoutVersion・スキーマ欄・作成 Union = active | declared・activation 複合エンドポイント〕+ `apps/server` の受理規則〔AUTH_SPEC §12-5 全項: declared 作成・activation 複合〔declared → active 専用ガード〕・レイアウト単調性 422 layout-regression・削除ステートメントのスキーマ欄直前一致・locked の作成時一回検査・unsupported-layout の最前段検査〕+ `schemaPolicy`〔§12-11 — 3 値・既定 disabled・disabled は v2 新規採用のみ拒否〔可逆性〕・監査 project.schema_policy_changed〕+ 配布面〔declared の値付き / メタのみ pull・リース応答への同梱、advisory 同梱〕。SELF_HOSTING "Updates" へ移行順序〔サーバー → 全メンバー CLI → enabled 化〕を追記)**。**S3 は 2026-09-01 実装完了(PR #121 — CLI の検証側 v2 対応〔レイアウト {1, 2} のサポート範囲検査は署名検証より前・v3〜 は「未対応レイアウト」の型付きエラー・declared のダイジェスト算入・§6.3 の値欠落 / 併置拒否・床の declared 対応〕+ `maruhi schema`〔表示 — agent-gate 許可側をテストで固定・escapeText 中和・非 TTY の枠付けヘッダ〕+ `maruhi schema set`〔部分更新・宣言作成〔既定 required = true・varType ""〕・v1 変数への最初の v2 再発行は --required / --optional 明示必須・locked / disabled advisory の署名前ローカル検査〕+ push の activation 複合 + run / ci run の presence fail-fast〔硬 — description をエラー文面に出さない〕・type advisory 警告 + エントロピー警告〔対話 = 確認 / 非対話 = --allow-high-entropy なしに拒否〕。schemaPolicy を設定する CLI コマンドは追加していない。既知の申し送り: スキーマ再発行の var.renamed 監査乖離〔オーナー裁定待ち〕・宣言の削除コマンド未提供〔S4 候補〕)**。**S4 は 2026-09-01 実装完了(PR #123 — `maruhi schema import`〔ブートストラップ儀式 4 段: 明示位置引数の .env / .env.example をクライアント側でのみ読む自前最小パーサ・変数ごとの対話承認〔y / e / s / q — 一括 --yes なし〕・承認分の declared 登録〔`requireCreation` — 並行作成レースで再発行へ黙って切り替えない〕・全候補宣言時のみ元ファイル削除提案。儀式系 deny〔既知エージェント + 非対話端末〕・エントロピー検出時は専用の明示確認・値は Redacted で包み型推論の形の観察のみ〔変数ごとの明示 y でのみ activation。忠実に解釈できない値は push 提案自体を抑制 = fail-closed〕〕+ `maruhi env diff` のスキーマ考慮〔required 軸・declared 注記・契約食い違い表示〔required と set / declared のみ・型は出さない〕— v1 変数は名前のみで required を捏造しない・description を出さない〕+ `maruhi var rm`〔S3 申し送りの採用 — v2 削除はスキーマ欄・レイアウトの直前 byte-exact 保持 / v1 は v1 形のまま・対話は変数名再入力の確認〔確認は variableId を束縛〕・非対話は --force 必須・3-F intent + 1-E′ 効果確認 + 床 tombstone 前進〕。crypto / server / api-schema 無変更 = 停止安全)**。**F′ の実測(session-46 §9 申し送りの解消 — 同 PR)**: 宣言 1 件 = ちょうど 3 往復(名前解決 pull + declared 作成複合 + 1-E′ 効果確認 pull)で時間の主項は 3N × RTT(RTT 100ms・N=100 で ~33s。RTT 0 の残項はクライアント検証コストで N に超線形)。**一括複合受理は不要を推奨**(import は対話儀式で人間の承認が常に律速・一括受理は複合原子性 / 監査 / CAS の受理面に踏み込み停止安全性を破る。再評価の契機 = 非対話バッチ経路の需要。大量 import の最初の改善レバーはクライアント側〔1-E′ 確認の隣接バッチ化・検証キャッシュ〕— オーナー判断待ち)。**S5 は 2026-09-01 実装完了(PR #125 — 系列 S0〜S5 完了): `maruhi schema export`〔検証済みステートメント集合から JSON Schema サブセットのスナップショットを stdout へ — リダイレクトでリポジトリへコミットする純生成物・正はストア。写像 = 1 スナップショット 1 環境・宣言型は標準語彙 1:1〔url = string + format "uri"・"" = 型キーワードなし〕・v1 変数は空スキーマで required 非算入〔捏造しない〕・declared / set〔値の有無〕は非収載〔契約のみ — 値の状態の正はライブの maruhi schema。activation で生成物を陳腐化させない〕・generated 枠付けは標準の $comment〔「verified」の語を使わない §14.3〕・決定的出力〔名前昇順・タイムスタンプなし〕〕+ `maruhi schema verify-snapshot`〔CI の乖離検査 — バイト一致判定 + 変数名・欄名のみの構造診断〔description の内容を端末レポートへ出さない〕・乖離 = exit 1・JSON Schema バリデータ不使用〕+ `maruhi schema lint`〔発見 G — JS/TS〔分割代入・optional chaining 込み〕/ Python / Go / Ruby / Rust の逐語 env 参照を自前正規表現で走査し検証済み live 名と突合。best-effort 注記の常時出力・レポートは変数名のみ・終了コードは非対称〔コードが読むが宣言なし = exit 1 / 宣言済みだが未読 = 報告のみ〕・--ignore で管理外変数を明示除外〕。3 コマンドとも鍵なしクラス〔メタのみ pull — MARUHI_TOKEN の CI で動く〕+ agent-gate 非適用〔許可側〕をテストで固定。crypto / server / api-schema 無変更 = 停止安全。SELF_HOSTING へ S5 節追記。既知の申し送りは S3 / S4 から不変〔var.renamed 監査乖離・一括複合受理 — オーナー裁定待ち〕)**。**申し送り 2 件の解消(2026-09-01 オーナー裁定)**: ① スキーマ再発行の var.renamed 監査乖離 = **`var.schema_reissued` の新設で解消**(案 B — 候補 8 案の比較・棄却は AUDIT_SPEC 1.3-draft の Status 欄。分岐は受理時の直前ステートメント名との byte 比較・名前とスキーマ欄の同時変更は var.renamed 1 行・可視性はクラス 1。ワイヤの event 型は開集合のため CLI 更新・順序要件なし。過去行は append-only のまま仕様に日付つき注記)、② 一括複合受理 = **不要で確定**(F′ 実測の推奨どおり。再評価の契機 = 非対話バッチ経路の需要 — は不変)。**順序の正式化(2026-08-30 H0 裁定 DB — session-46 §9 申し送りの解消): S1〜S3(署名・受理・CLI 検証面)は招待制ベータ開放前に着地する(H 系列のゲート)。S4〜S5(import / export / lint — 生成物・付帯 UX)はゲートにせずベータと並走可**
- MCP サーバー同居(スコープ付き・短命・監査付きの読み取り。値なしスキーマの配信機構を兼ねる — スキーマの MCP 配信は需要の実測後に `maruhi schema` の薄いラッパとして追加〔2026-08-30 オーナー既決 — 検討メモ §1〕)
- エージェント向け credential brokering(`maruhi proxy run`: プレースホルダのみ渡し、通信境界で実値に差し替え。「サーバーもエージェントも平文を持たない」)。**短命化コネクタ(2026-09-04 追加 — docs/notes/integration-options.md §6 D3)**: 差し替える値を長期キーでなく、その場で発行した短命トークン(GitHub App のインストールトークン・OAuth の access トークン・AWS STS)にする。発行者は各人の機械のプロキシで、サーバーは関与しない — Infisical Agent Proxy(サーバーが平文を持つ)に対する上位互換。コネクタ枠はローテーション(需要駆動節)と共用
- DO ベースのリース(「このエージェントセッションに、この変数だけ、30 分」)
- no-reveal 方針化(人間向けの値表示を例外操作に格上げ。エージェント検出時の表示拒否は既定のまま)
- リーク検知・ログ redact(付帯機能。本線を汚さない範囲で)

## 需要駆動(ベータでの観測後に着手 — 2026-09-04 追加)

競合比較(docs/notes/competitive-analysis.md)で劣後と判定した項目のうち、競合自身が有料上位にしか置いていないもの。ゲートにはせず、招待制ベータ以降の声で着手順を決める。案の列挙・評価・棄却理由は docs/notes/integration-options.md。横断の共通部品は同メモ §7(変更通知 webhook = SY6・コネクタ枠・封印提案)— **いずれも機械メンバー(サービスアカウント)を導入せず「署名するのは人間だけ」を保つ**。

- 上流ローテーション(最小形。概算 3〜4 週): R1 発行元ごとの手順書 + R2 `maruhi var rotate <var>`(CLI 実行。コネクタ枠 + AWS IAM アクセスキー / Postgres・MySQL パスワード / Cloudflare API トークンの 3 つ。二重資格の猶予 → `--finalize` で旧資格の無効化。発行元の管理者資格は maruhi の変数としてメモリで取り出す)+ R3 `member remove` 直後のチェックリスト(その人が復号できた環境の上流付き変数を列挙して R2 を促す — ADR-0014 決定 3「人間エラーは機構で守る」の具体形)+ R9 期限つき値(スキーマ欄への `expires_at` 相当 — **CRYPTO_SPEC §4.2 改訂・テストベクター先行・人間レビュー**)。定期自動化は封印提案(CI が候補値を現メンバー公開鍵へ HPKE 封印して置き、人間が副署 — CRYPTO_SPEC §5 の範囲で追記)で後続。**サーバー駆動(サーバーへの署名鍵付与)と CI への長期署名鍵配置はやらない**。docs には「静的資格をなくす = クラウドのフェデレーション(OIDC → STS / WIF)」を公式見解として先に書く(同メモ R7)
- SDK(TypeScript のみ。概算 3〜4 週): 既存ワークロードリース(CRYPTO_SPEC §9.1 — issuer 汎用)のクライアント。契機 = 「同期先に平文を置きたくない」の声(Vercel OIDC 等の workload identity を出せる環境)。CLI の検証コード(チェーン・アンカー・マニフェスト・チェックポイント・リース)を別パッケージへ切り出す境界設計 + サーバーの OIDC 検証を GitHub 以外の issuer へ一般化(AUTH_SPEC §14 追記)+ ステートレスなランタイムでのアンカー更新の設計。**多言語の検証付き SDK・平文を返す SDK(ホステッド)・フレームワークプラグインはやらない**。値なしのメタデータ SDK(HttpApi 導出クライアント)は公開 API として整備してよい
- 動的シークレット: **サーバー発行はやらない(2026-09-04 決定 — 同メモ §6 D1。発行元の管理者資格をサーバーに預ける形はゼロ知識の外)**。公式見解「クラウド資格は OIDC → STS / WIF で取り、maruhi は残りの静的な秘密を持つ」を脅威モデル文書 / docs の "What maruhi does not do" に書く(H5 と同時)。クライアント側の短命資格は Phase 3 の brokering(短命化コネクタ)に統合済み。使い捨て資格(`maruhi run --ephemeral <connector>` — 各人の機械が自分専用の短命 DB ユーザー等を発行し終了時に消す)は R2 のコネクタ枠の後
- SSO / SCIM: GA の Enterprise 問い合わせ枠で需要を計測し、来たら実装(AUTH_SPEC §7 の WorkOS 挿入点)。SCIM の自動退会はチェーン署名(`remove_member`)が要るためサーバーは実行できず、「退会候補のフラグ + admin の CLI への促し」まで
- 値あり GUI: **やらない(2026-09-04 所有者裁定)**。値なし `maruhi ui`(ADR-0018 第 2 段)を先に出し、招待制ベータで「画面で値を見たい」が主要な離脱理由として繰り返し観測された場合のみ再訪(同メモ §2 の再訪条件)

## 将来(未スケジュール)

- **分散型の発想から(2026-09-05 所有者裁定 — 将来やる価値があるものとして記載。案の評価は integration-options.md §3 補足 17)**:
  - **チェーン上の複数署名承認**: `grant_server` / `remove_member` 等の危険操作を owner 2 名の署名で受理する「提案 → 承認」の 2 段エントリ(CRYPTO_SPEC §6.2 改訂・全クライアントの検証規則が変わる・テストベクター先行。概算 3〜4 週)。「1 人では危険な操作ができない」を暗号で保証する。上記「四眼」の実装形の本命。Enterprise 枠の中身の候補
  - **チェーンヘッドの公開証人**: 署名済みヘッド申告をユーザー所有の公開追記ログ(git リポジトリ等)へ置き、サーバーの split view(CRYPTO_SPEC §14.3-4)を第三者が検出可能にする(CLI に書き込み / 照合の 2 コマンド。概算 1 週)。送信先はユーザー設定に限定(テレメトリゼロと整合)
  - **ミラー(自分の CF アカウントの読み取りレプリカ)= 可用性の答え**: ホステッドが落ちても `run` / `ci run` / デプロイ時再適用が自分のミラーから読める。**回避できるのは読み取り側(デプロイが止まる問題)で、書き込み(push・チェーン追記)は正の復旧を待つ**(書き込みは人間の操作なので許容)。CI リースをミラーから出すには、ミラーのサーバー鍵へ `grant_server` する(自分のサーバーなので許容済み)。クライアントは設定に「正 → ミラー」のフォールバック順を持つ。仕様改訂(サーバー鍵の一意性まわり)+ レプリケーション経路。ミラーがない構成の可用性は非保証(G8)のまま脅威モデル文書に明記する
  - 後回し: 組織の鍵台帳(org 内の招待を儀式不要にする mini-PKI — Enterprise 枠候補)、可搬な監査証跡(`audit export` — 監査人がサーバーなしで検証)、署名付き委任(macaroon 型 — bearer トークンの代替。リースと役割が重なる)、鍵ログイン(メンバー鍵でセッション)、2-of-2 二重ラップ(本番値の閲覧に 2 人 — 四眼 reveal の暗号的実装形)、自己監査エンドポイント(「サーバーが他人に見せている私の公開鍵」を手元と照合。1〜2 日)、ビルドハッシュの申告(ヘッド申告に CLI の自ビルドハッシュを添え、チーム全員が同じ検証済みビルドかを確かめる)。**鍵の透明性ログは独立には作らない** — maruhi ではチェーンが鍵の台帳であり、KT ≒ 公開証人 + 自己監査(integration-options.md §3 補足 17 末尾)。招待儀式は据え置き
- パスキー PRF によるデバイス鍵(封印バックエンド抽象の検討メモ: docs/notes/device-key-sealing.md)、SOPS 互換エクスポート、`maruhi/v2` ハイブリッド PQ(~~ホステッドクラウド版~~ は 2026-08-30 H0 で実スケジュール化 — 上記 H 系列の節。WorkOS 再判断は実施済み = GitHub 直実装続行 — ADR-0009)
- リカバリーの封印バックアップ(ユーザー所有ストレージへ、パスフレーズ / パスキーで暗号化。運営が読める預かりは禁止 — ADR-0014)
- 四眼・承認付き reveal(prod reveal / エクスポート / 危険操作の break-glass。企業向け)、上流 credential の自動ローテーション(→ 2026-09-04 に「需要駆動」節へ具体化。ここに残るのは定期自動化 = 封印提案の形と、参照型変数〔`aws-sm://…` のポインタ — integration-options.md R8〕の検討のみ)、remove / 降格から全環境 rotate 完了までの窓の機構化(CRYPTO_SPEC §14.3-5 (ii)。候補はセッション 12 ノート §10-7)
