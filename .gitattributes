# Schema snapshots regenerated by drizzle-kit (the review target is migration.sql)
apps/server/drizzle/*/snapshot.json linguist-generated=true

# Source is always diffed as text (prevents the incident where mixed control
# bytes make git treat a file as binary and behavior changes stop showing up
# in diffs — PR #71 review feedback)
*.ts diff
*.tsx diff
