{
  "name": "maruhi",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "type": "module",
  "scripts": {
    "check": "bun run fmt:check && bun run lint && bun run typecheck && bun run importlint && bun run fallow:audit && bun run doctor && bun run test",
    "doctor": "bun run --filter @maruhi/web doctor",
    "fallow:audit": "fallow audit",
    "fallow:baseline": "fallow dead-code --save-baseline fallow-baselines/dead-code.json && fallow health --save-baseline fallow-baselines/health.json && fallow dupes --save-baseline fallow-baselines/dupes.json",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "importlint": "import-lint",
    "lint": "oxlint",
    "test": "vitest run",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "@import-lint/cli": "0.1.6",
    "fallow": "3.10.0",
    "oxfmt": "0.61.0",
    "oxlint": "1.76.0",
    "oxlint-plugin-eslint": "1.76.0",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  },
  "engines": {
    "bun": "1.3.14"
  }
}
