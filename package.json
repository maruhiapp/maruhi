{
  "name": "maruhi",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "type": "module",
  "scripts": {
    "check": "bun run fmt:check && bun run lint && bun run typecheck && bun run importlint && bun run fallow:audit && bun run doctor && bun run test",
    "doctor": "bun run --filter @maruhi/web doctor",
    "fallow:audit": "fallow audit",
    "fallow:baseline": "fallow dead-code --save-baseline fallow-baselines/dead-code.json && fallow health --save-baseline fallow-baselines/health.json --baseline-mode identity && fallow dupes --save-baseline fallow-baselines/dupes.json",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "importlint": "import-lint",
    "lint": "oxlint",
    "test": "vitest run",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "@import-lint/cli": "0.1.9",
    "fallow": "3.21.0",
    "oxfmt": "0.65.0",
    "oxlint": "1.80.0",
    "oxlint-plugin-eslint": "1.80.0",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  },
  "overrides": {
    "@types/node": "26.4.0"
  },
  "engines": {
    "bun": "1.4.0"
  }
}
